@model HomeContent
<div class="container">
    <div class="row">
        <div class="col-lg-11 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Home Content area</h4>
                    <p class="card-description">
                    </p>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Title</th>
                                <th>Description</th>
                                <th>Setting</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                
                                <td>@Model.Title</td>
                                <td>@(Model.Description.Length > 50 ? Model.Description.Substring(0, 50) : Model.Description)</td>
                                <td>
                                    <a class="text-succes pe-2 text-decoration-none" asp-action="update" asp-route-id="@Model.Id">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                </td>
                            </tr>
                           
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>